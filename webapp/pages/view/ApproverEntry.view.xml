<mvc:View
	controllerName="zwfuol.pages.controller.ApproverEntry"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m"
	xmlns:c="sap.ui.core"
	xmlns:t="sap.ui.table"
	xmlns:l="sap.ui.layout"
	displayBlock="true"
	height="100%"
	busyIndicatorDelay="0"
>
	<l:VerticalLayout class="sapUiContentPadding">
		<t:Table
			id="initiatortbl"
			rows="{
				path: 'tblData>/InitiatorGroup',
				sorter: {
					path: 'Group'
				}
			}"
			selectionMode="MultiToggle"
			visibleRowCount="7"
			paste="onPaste"
			ariaLabelledBy="title"
			rowSelectionChange="onTableSelectionChanged"
		>
			<t:extension>
				<OverflowToolbar style="Clear">
					<Title
						id="title"
						text="{i18n>Label.RegisterInitiator}"
					/>
					<ToolbarSpacer />
					<Button
						icon="sap-icon://group"
						textDirection="Inherit"
						type="Default"
						press="onManageGroup"
					/>
					<Button
						icon="sap-icon://save"
						textDirection="Inherit"
						type="Default"
						press="onSave"
						visible="{viewData>/showSave}"
					/>
					<Button
						icon="sap-icon://delete"
						textDirection="Inherit"
						type="Default"
						press="onDelete"
						visible="{viewData>/showDelete}"
					/>
					<Button
						icon="sap-icon://add"
						textDirection="Inherit"
						type="Default"
						press="onAdd"
					/>
				</OverflowToolbar>
			</t:extension>
			<t:columns>
				<t:Column
					width="12rem"
					filterProperty="Groupid"
				>
					<Label text="{i18n>Label.GroupID}" />
					<t:template>						
						<Input
							placeholder="{i18n>Select.InitiatorGroup}"
							showSuggestion="true"
							suggestionItems="{/InitGroupSet}"
							value="{tblData>Groupid}"
						>
							<suggestionItems>
								<c:ListItem
									text="{Groupid}"
									additionalText="{Groupname}"
								/>
							</suggestionItems>
						</Input>
					</t:template>
				</t:Column>
				<t:Column
					width="10rem"
					filterProperty="Useragent"
				>
					<Label text="{i18n>Label.UserID}" />
					<t:template>
						<!-- <Input
							value="{
								path: 'tblData>Useragent'
							}"
							maxLength="12"
						/> -->

						<Input
							placeholder="{i18n>Select.InitiatorGroup}"
							maxLength="12"
							showSuggestion="true"							
							suggestionItems="{/UserVHSet}"
							value="{tblData>Useragent}"
						>
							<suggestionItems>
								<c:ListItem
									text="{Bname}"
									additionalText="{Department}"
								/>
							</suggestionItems>
						</Input>
					</t:template>
				</t:Column>
				<t:Column width="10rem">
					<Label text="{i18n>Label.ValidFrom}" />
					<t:template>
						<DatePicker value="{
							path: 'tblData>Validfrom',
							type: 'sap.ui.model.type.Date',
							formatOptions: {
								pattern: 'yyyy-MM-dd'
							}
						}" />
					</t:template>
				</t:Column>
				<t:Column
					width="10rem"
					filterProperty="Areasize"
				>
					<Label text="{i18n>Label.ValidTo}" />
					<t:template>
						<DatePicker value="{
							path: 'tblData>Validto',
							type: 'sap.ui.model.type.Date',
							formatOptions: {
								pattern: 'yyyy-MM-dd'
							}
						}" />
					</t:template>
				</t:Column>
			</t:columns>
		</t:Table>
	</l:VerticalLayout>
</mvc:View>